/** GridFS bucket names (MongoDB only — no Appwrite). */
export const GRIDFS_BUCKETS = {
  certificates: "certificates",
  templatesJson: "templates_json",
  templatesCover: "templates_cover",
  avatars: "avatars",
} as const;
